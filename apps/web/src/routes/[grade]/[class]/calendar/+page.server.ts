// Class-scoped calendar view. School calendar is school-global, so this reuses
// the same loader as /calendar (school is resolved from the parent layout).
export { load } from '../../../calendar/+page.server';
