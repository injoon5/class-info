// Class-scoped meals view. Meals are school-global, so this reuses the same
// loader as /meals (school is resolved from the parent layout either way).
export { load } from '../../../meals/+page.server';
