# Local/dev. Vercel Preview/Production: leave unset — `npx convex deploy --cmd`
# injects PUBLIC_CONVEX_URL for that deployment. Preview DBs start empty; the
# Vercel build hydrates meals/timetable/schedule from the school APIs.
PUBLIC_CONVEX_URL=https://placeholder.convex.cloud
