PUBLIC_CONVEX_URL=
# Optional: in local dev, force a school subdomain when visiting plain
# `localhost` (e.g. the seeded school's subdomain). In production the
# subdomain is derived from the Host header instead.
PUBLIC_DEV_SUBDOMAIN=
