# Local/dev only. Do not set this on Vercel Preview or Production —
# `npx convex deploy --cmd` injects CONVEX_URL / PUBLIC_CONVEX_URL for that
# deployment. A hardcoded Vercel value pins every preview to production Convex.
PUBLIC_CONVEX_URL=https://placeholder.convex.cloud
